<Page
    x:Class="ProjekatMEDICA.IzbrisiProizvod"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjekatMEDICA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <RelativePanel>
            <TextBlock Name="Id" Text="Naziv ili ID:" FontSize="18" FontWeight="Normal" Margin="60,50,0,10"></TextBlock>
            <TextBox Text ="{x:Bind viewModel.naziv}" Name="Naziv" MinWidth="200" RelativePanel.RightOf="Id" Margin="10,50,0,10"></TextBox>
            <Button Command ="{Binding Path = viewModel.pretragaBtn}" Name="Pretraga" Content="Pretraga" FontSize="18" FontWeight="Normal" RelativePanel.RightOf="Id" Margin="10,120,0,10"></Button>
            <ListBox Name="Lista" Margin="60, 190,0,10" Height="100" Width="250"></ListBox>
            <Button Command ="{Binding Path=viewModel.potvrdiBtn}" Name="Potvrdi" Content="Potvrdi" FontSize="18" FontWeight="Normal" Margin="235,300,20,0"></Button>
        </RelativePanel>
    </Grid>

</Page>
